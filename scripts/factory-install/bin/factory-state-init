#!/usr/bin/env bash
set -euf -o pipefail
factory_install_sh_lib_dir="$("$(dirname "$0")/pkg-nsf-factory-install-get-sh-lib-dir")"
. "$factory_install_sh_lib_dir/cli_factory_state.sh"

init_factory_state_cli "$@"
