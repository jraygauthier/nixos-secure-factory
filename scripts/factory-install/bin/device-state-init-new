#!/usr/bin/env bash
set -euf -o pipefail
factory_install_libexec_dir="$("$(dirname "$0")/pkg-nixos-sf-factory-install-get-libexec-dir")"
. "$factory_install_libexec_dir/cli_device_state.sh"

init_new_device_state_cli "$@"
