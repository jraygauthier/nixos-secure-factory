#!/usr/bin/env bash
set -euf -o pipefail
factory_install_sh_lib_dir="$("$(dirname "$0")/pkg-nixos-sf-factory-install-get-sh-lib-dir")"
. "$factory_install_sh_lib_dir/cli_device_state.sh"

init_new_device_state_cli "$@"
