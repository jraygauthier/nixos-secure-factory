#!/usr/bin/env bash
set -euf -o pipefail
common_factory_install_libexec_dir="$("$(dirname "$0")/pkg-nixos-sf-factory-common-install-get-libexec-dir")"
. "$common_factory_install_libexec_dir/cli_device_system.sh"

build_device_system_update_bundle_locally_cli "$@"
