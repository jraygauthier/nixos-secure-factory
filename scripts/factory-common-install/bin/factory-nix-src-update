#!/usr/bin/env bash
set -euf -o pipefail
common_factory_install_sh_lib_dir="$("$(dirname "$0")/pkg-nixos-sf-factory-common-install-get-sh-lib-dir")"
. "$common_factory_install_sh_lib_dir/nix_src_update.sh"

update_nix_src_json_cli "$@"
