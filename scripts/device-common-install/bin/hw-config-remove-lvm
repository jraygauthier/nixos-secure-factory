#!/usr/bin/env bash
set -euf -o pipefail
# DEPRECATED tool. Use 'hw-config-boot-device-remove-lvm' instead.
declare script_dir
script_dir="$(cd "$(dirname "$0")" > /dev/null && pwd)"
"$script_dir/hw-config-boot-device-remove-lvm" \
  "/dev/sda1"
