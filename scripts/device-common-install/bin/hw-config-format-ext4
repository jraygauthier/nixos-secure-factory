#!/usr/bin/env bash
set -euf -o pipefail
# DEPRECATED tool. Use 'hw-config-boot-device-format-ext4' instead.
declare script_dir
script_dir="$(cd "$(dirname "$0")" > /dev/null && pwd)"
"$script_dir/hw-config-boot-device-format-ext4" \
  "/dev/sda" "nixos"
